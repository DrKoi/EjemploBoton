import 'package:flutter/material.dart';

class NcPage2 extends StatelessWidget {
  const NcPage2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Pagina 2'),
    );
  }
}
