import 'package:flutter/material.dart';

class NcPage3 extends StatelessWidget {
  const NcPage3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Pagina 3'),
    );
  }
}
